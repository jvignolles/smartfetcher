== Smart Fetcher

A simple application to fetch Facebook pages and feeds.

Require Ruby 2+.

Configuration: copy `config/application.yml.default` to `config/application.yml` and fill it with correct data.

